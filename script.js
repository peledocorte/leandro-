// Este arquivo pode ser usado para adicionar funcionalidades como:
// - Mudar a velocidade (animation-duration) dinamicamente.
// - Alterar a frase motivacional.
// - Usar a API requestAnimationFrame para um movimento JS em vez de CSS (não recomendado para este caso).

/* Exemplo de como mudar a velocidade via JS:
const marqueeText = document.querySelector('.marquee-text');
const novaVelocidade = '10s'; // Por exemplo, 10 segundos
marqueeText.style.animationDuration = novaVelocidade;
*/